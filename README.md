# Beam Search
Ensure you have dependencies with:
```
uv sync
```

Assuming you have a cipher (no specified folder, put in src), you can run:

```
uv run main.py
```

Adjustments to beam width, cipher, and N-max can be made in main-py.